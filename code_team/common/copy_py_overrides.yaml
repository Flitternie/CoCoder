# .py files that must be copied to workspace (not generated by AI).
# These are data files or auto-generated code that happen to have .py extension.
# Patterns are relative to the repo root and support glob syntax.

CodeProjectEval/djangorestframework-simplejwt:
  - "rest_framework_simplejwt/token_blacklist/migrations/*.py"

CodeProjectEval/zxcvbn:
  - "zxcvbn/frequency_lists.py"
  - "zxcvbn/adjacency_graphs.py"
